dist
node_modules
.astro
public
# Docs are the source of truth in ./docs/; src/content/docs is a build artefact
# written by scripts/sync-docs.js.
src/content/docs
package-lock.json

# Hand-authored documentation prose. Formatting it churns 100+ files and
# normalises markdown the authors chose deliberately; run prettier on it only
# on purpose, never as part of `npm run verify`.
docs

# Content and internal notes: hand-authored prose whose markdown was chosen
# deliberately. Same reasoning as docs/ above.
src/content/releases
internal

# Tooling with its own life. registry.json in particular is generated by
# seed-registry.mjs, so reformatting it here would churn on every regeneration.
docs-sync
