import type { LocaleMeta } from '@/i18n/types';

export const meta: LocaleMeta = {
  htmlLang: 'en',
  // en_GB, not en_US: the copy is British ("organisation", "colour") and the
  // site says Made in Europe on every page.
  ogLocale: 'en_GB',
  skipToContent: 'Skip to content',
};
