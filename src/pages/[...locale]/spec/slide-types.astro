---
import SpecTypesPage from '@/components/pages/spec/SpecTypesPage.astro';
import { localeParams, type Lang } from '@/i18n';

export function getStaticPaths() {
  return localeParams();
}

const { lang } = Astro.props as { lang: Lang };
---

<SpecTypesPage lang={lang} />
