---
import HostingPage from '@/components/pages/HostingPage.astro';
import { localeParams, type Lang } from '@/i18n';

export function getStaticPaths() {
  return localeParams();
}

const { lang } = Astro.props as { lang: Lang };
---

<HostingPage lang={lang} />
