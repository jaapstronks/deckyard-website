---
import StructuredPage from '@/components/pages/StructuredPage.astro';
import { localeParams, type Lang } from '@/i18n';

export function getStaticPaths() {
  return localeParams();
}

const { lang } = Astro.props as { lang: Lang };
---

<StructuredPage lang={lang} />
